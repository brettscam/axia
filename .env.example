# Supabase project credentials (safe for browser — these are public keys)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# NOTE: Anthropic and ATTOM API keys do NOT go here.
# They are stored as Supabase Edge Function secrets.
# See CLAUDE.md section 3 — Security Non-Negotiables.
